# ssh-aws-sso-login
A CLI utility that performs `aws sso login` on a remote server via SSH, automatically forwards the authentication callback port to localhost, and opens the default browser for the user to complete the SSO authentication flow.
